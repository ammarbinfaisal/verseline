services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: verseline
      POSTGRES_USER: verseline
      POSTGRES_PASSWORD: verseline
    ports:
      - "5434:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
